# css-practice-Day-4
# CSS Practice - Day 4

## What I Learned Today
- **CSS Selectors**: `h1`, `*`, `.mern`, `input` styling
- **Pseudo-elements**: `::before` and `::after` used on `#first` 
- **Pseudo-class**: `:hover` effect on `#first`
- **Google Fonts**: Imported Roboto via `@import`
- **Input Styling**: Added padding, border, border-radius
- **Injected Content**: Used `::before` to add "Welcome to the MERN stack!" text

## Tech Stack
HTML5 + CSS3
